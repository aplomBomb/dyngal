export interface RefDimsType {
  refDims: {
    fullWidth: number;
    fullHeight: number;
    halfWidth: number;
    halfHeight: number;
    quarterWidth: number;
    quarterHeight: number;
  };
}
