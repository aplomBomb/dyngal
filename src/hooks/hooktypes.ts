export interface WindowDimensionType {
  windowDims: {
    fullWidth: number;
    fullHeight: number;
    halfWidth: number;
    halfHeight: number;
    quarterWidth: number;
    quarterHeight: number;
  };
}

export interface RefDimsType {
  refDims: {
    fullWidth: number;
    fullHeight: number;
    halfWidth: number;
    halfHeight: number;
    quarterWidth: number;
    quarterHeight: number;
  };
}
